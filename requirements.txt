# Core Framework and Server
fastapi>=0.110.0
uvicorn[standard]==0.29.0

# Data Validation and Type Handling (Comes with FastAPI, but good to be explicit)
pydantic==2.7.1

# HTTP Requests for the Checker Function
requests==2.32.4
